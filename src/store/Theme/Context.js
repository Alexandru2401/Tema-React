// Creeam contextul pt tema aplicatiei - el va fii asociat cu store-ul de timp - folosim createContext din React

import { createContext } from "react";

export const ThemeContext = createContext();